name: umeng_push_example
description: Demonstrates how to use the umeng_push_sdk plugin.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

environment:
  sdk: ^3.10.0

dependencies:
  flutter:
    sdk: flutter

  umeng_push_sdk:
    #    ^2.4.0
    path: ../

  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
